"""Canonical resource types re-exported from the frozen product ports."""

from ..ports import ResourceDescriptor, ResourceKind, ResourceSource

__all__ = ["ResourceDescriptor", "ResourceKind", "ResourceSource"]
